# Examples

These examples show unrelated relying products validating Delegent authority
proofs locally without any commercial service.

Run them from the repository root with:

```bash
PYTHONPATH=src python examples/document_review_gate.py
PYTHONPATH=src python examples/maintenance_window_gate.py
```
